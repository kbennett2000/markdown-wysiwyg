// Runtime configuration. In the Docker image this file is rewritten at container
// start from the GDRIVE_CLIENT_ID environment variable. Empty = no Client ID
// configured (the optional Google Drive feature then prompts for one).
window.MDEDIT_GDRIVE_CLIENT_ID = "";
